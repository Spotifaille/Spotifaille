services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./src:/var/www/html
    depends_on:
      - db
    environment:
      - DB_HOST=db
      - DB_USER=root
      - DB_PASSWORD=rootpassword
      - DB_NAME=Spotifaille
      - MONGO_URI=mongodb://root:rootpassword@27017
    networks:
      - spotinet
    restart: on-failure

  db:
    image: mongo:latest
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=rootpassword
      - MONGO_INITDB_DATABASE=Spotifaille
    volumes:
      - ./init-mongo.js:/docker-entrypoint-initdb.d/init-mongo.js:ro
      - db-data:/data/db
    ports:
      - "27017:27017"
    networks:
      - spotinet
    restart: on-failure

volumes:
  db-data:

networks:
  spotinet:
